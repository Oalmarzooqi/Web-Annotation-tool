# OCT frontend (Vite)

React + TypeScript + Vite app for the OCT annotator. See the [repository root README](../README.md) for how to run the full stack, project layout, and annotate features.

**Dev server** (from repo root):

```bash
./run.sh frontend
```

**Lint and build**:

```bash
npm run lint
npm run build
```
